# 인게임 에디터 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 메뉴에서 진입하는 인게임 에디터로 스테이지·밸런스·리소스를 편집하고, localStorage 즉시 적용 + JSON export/import한다.

**Architecture:** 순수 데이터 레이어 `editor-core.jsx`(`BAL` 밸런스 설정 + `applyOverrides`/직렬화/`validateStage`)와 UI `editor.jsx`(`EditorScreen` 3탭)로 분리. 부팅 시 localStorage 패치를 `window.HXS.STAGES`·`window.HXR.RES`·`window.HXB`에 병합해 게임·에디터가 동일 데이터를 본다. 데이터 레이어는 `node:vm` 하니스로 TDD, UI는 브라우저 수동 검증.

**Tech Stack:** React 18 UMD + Babel-in-browser (no build), SVG, `node:test` + `node:vm`.

> **선행 의존:** 확장팩 1탄 플랜([2026-05-31-expansion-pack-1.md](2026-05-31-expansion-pack-1.md))의 **Phase 1~6**(엔진 메커니즘: `ENEMY_KINDS`, crack/pad, 탄 `vc`/`fuse`, 보스 신패턴, 신규 스프라이트)가 **이미 구현돼 있어야 한다.** 스테이지 탭 팔레트가 이 기믹들을 편집 대상으로 삼는다.

> **git:** 이 폴더는 git 저장소가 아니다. "Commit" 스텝은 `git init` 한 경우만 적용. 아니면 각 Task 종료 시 테스트 초록 + 수동 검증을 게이트로 삼는다.

---

## File Structure

| 파일 | 책임 | 생성/수정 |
|------|------|-----------|
| `editor-core.jsx` | 순수 데이터: `DEFAULT_BAL` 병합, `applyOverrides`, serialize/parse/import, `validateStage` | 생성 |
| `editor.jsx` | `EditorScreen` + Stage/Balance/Resource 탭 UI | 생성 |
| `engine.jsx` | `DEFAULT_BAL` 정의·export + 튜닝 리터럴을 `bal()` 읽기로 전환 | 수정 |
| `stages.jsx` | `initStageDef(def, idx)` 분리, `isUnlocked` 커스텀(id≥1000) 허용 | 수정 |
| `screens.jsx` | `MenuScreen`에 "에디터" 버튼 | 수정 |
| `app.jsx` | `editor` 화면 라우팅 + 테스트 플레이 진입/복귀(별점 미저장) | 수정 |
| `Hex Danmaku.html` | `editor-core.jsx`·`editor.jsx` script 태그 | 수정 |
| `styles.css` | 에디터 UI 스타일 | 수정 |
| `tests/harness.mjs` | `loadEditor()` 헬퍼(fake localStorage + RES stub) 추가 | 수정 |
| `tests/overrides.test.mjs` | 데이터 레이어 테스트 | 생성 |
| `docs/hex-danmaku-dev.md` | 에디터·오버라이드·BAL 문서화 | 수정 |

---

## Phase E1 — BAL 밸런스 설정 + 엔진 전환

### Task 1: `DEFAULT_BAL` 정의 + 튜닝 값 BAL 읽기로 전환

**Files:**
- Modify: `engine.jsx`

> 목표: 기본값을 현재 리터럴과 **동일**하게 둬서, 확장팩 회귀 테스트가 그대로 초록이어야 한다.

- [ ] **Step 1: `DEFAULT_BAL` + `bal()` 추가 (engine.jsx 상단, 그리드 상수 아래)**

```js
// ─── Balance config (editor-tunable; engine reads via bal()) ───
const DEFAULT_BAL = {
  skill: { undoCost: 30, bombCost: 50, bombRadius: 2, freezeCost: 80, freezeTurns: 3 },
  score: { surviveBase: 10, comboCap: 10, gemBase: 80, gemCombo: 4, starBase: 50, starCombo: 3 },
  item:  { spawnChance: 0.24, max: 3, pSc: 0.45, pBm: 0.18, pTp: 0.12 }, // ht = 나머지
  enemy: { chaseEvery: 2, lungeWindup: 1, lungeDash: 2 },
  endless: { diffEasy: 15, diffNormal: 35, diffHard: 60 },
};
// window.HXB는 editor-core가 세팅. 미설정(엔진 단독/테스트) 시 기본값 폴백.
const bal = () => (typeof window !== 'undefined' && window.HXB) ? window.HXB : DEFAULT_BAL;
```

- [ ] **Step 2: `DL` 임계값 전환 (engine.jsx `DL`)**

```js
const DL = (t) => {
  const e = bal().endless;
  return t < e.diffEasy   ? { lb: 'EASY',   sub: '초급', c: '#5eead4' } :
         t < e.diffNormal ? { lb: 'NORMAL', sub: '중급', c: '#fbbf24' } :
         t < e.diffHard   ? { lb: 'HARD',   sub: '고급', c: '#fb7185' } :
                            { lb: 'CHAOS',  sub: '극한', c: '#f43f5e' };
};
```

- [ ] **Step 3: `tryItem` 확률 전환 (engine.jsx `tryItem`)**

`if (its.length >= 3)` → `if (its.length >= bal().item.max)`,
`if (Math.random() > 0.24)` → `if (Math.random() > bal().item.spawnChance)`,
타입 분기:

```js
  const it = bal().item;
  const ty = roll < it.pSc ? 'sc'
           : roll < it.pSc + it.pBm ? 'bm'
           : roll < it.pSc + it.pBm + it.pTp ? 'tp'
           : 'ht';
```

- [ ] **Step 4: `tick` 점수/콤보/콤보캡 전환 (engine.jsx)**

- 별 점수: `bonus = 50 + combo * 3;` → `bonus = bal().score.starBase + combo * bal().score.starCombo;`
- gem: `const gv = 80 + combo * 4;` → `const gv = bal().score.gemBase + combo * bal().score.gemCombo;`
- 생존 점수: `sc += (10 + Math.min(combo, 10)) + bonus;` → `sc += (bal().score.surviveBase + Math.min(combo, bal().score.comboCap)) + bonus;`

- [ ] **Step 5: 스킬 비용/반경/턴 전환 (engine.jsx doUndo/doBomb/doFreeze)**

```js
const doUndo = (s) => {
  const c = bal().skill.undoCost;
  return (!s.hist || s.sc < c || s.ov || s.win) ? s
    : { ...s.hist, sc: s.sc - c, hist: null, ov: false, win: false, evts: [], skillUses: (s.skillUses || 0) + 1 };
};
const doBomb = (s) => {
  const { bombCost: c, bombRadius: rad } = bal().skill;
  if (s.sc < c || s.ov || s.win) return s;
  const xc = s.bl.filter(b => hd(s.pl.r, s.pl.c, b.r, b.c) <= rad);
  return { ...s,
    bl: s.bl.filter(b => hd(s.pl.r, s.pl.c, b.r, b.c) > rad),
    enemies: (s.enemies || []).filter(e => hd(s.pl.r, s.pl.c, e.r, e.c) > rad),
    sc: s.sc - c, skillUses: (s.skillUses || 0) + 1,
    evts: [{ ty: 'bm', r: s.pl.r, c: s.pl.c, cells: xc.map(b => `${b.r},${b.c}`) }] };
};
const doFreeze = (s) => {
  const { freezeCost: c, freezeTurns: ft } = bal().skill;
  return (s.sc < c || s.fz > 0 || s.ov || s.win) ? s
    : { ...s, fz: ft, sc: s.sc - c, skillUses: (s.skillUses || 0) + 1, evts: [] };
};
```

- [ ] **Step 6: 적 cadence/lunge 상수 전환 (engine.jsx `ENEMY_KINDS`, 확장팩에서 추가된 코드)**

`chase.step`의 `if (ctx.t % 2 !== 1) return;` → `if (ctx.t % bal().enemy.chaseEvery !== bal().enemy.chaseEvery - 1) return;`
`LUNGE_WINDUP`/`LUNGE_DASH` 상수 사용처를 `bal().enemy.lungeWindup`/`bal().enemy.lungeDash`로 교체(`lunge.step`, `telegraph`). 모듈 상수 `LUNGE_WINDUP`/`LUNGE_DASH`는 제거하거나 기본값 동기화.

- [ ] **Step 7: export에 `DEFAULT_BAL` 추가 (engine.jsx `Object.assign(window, { HX: {...} })`)**

`initState,` 다음 줄에 `DEFAULT_BAL,` 추가.

- [ ] **Step 8: 회귀 테스트 → 전부 통과 (값 불변)**

Run: `npm test`
Expected: PASS. 기본값이 기존 리터럴과 같으므로 모든 기존/확장팩 테스트가 그대로 통과해야 한다. 실패하면 기본값 오타.

- [ ] **Step 9: Commit** *(git)*

```bash
git add engine.jsx
git commit -m "refactor(engine): route tunable values through window.HXB (DEFAULT_BAL fallback)"
```

---

## Phase E2 — 데이터 레이어 (editor-core)

### Task 2: 하니스에 `loadEditor()` 추가

**Files:**
- Modify: `tests/harness.mjs`

- [ ] **Step 1: fake localStorage + RES stub 로더 추가**

`tests/harness.mjs`에 추가:

```js
// load engine + stages + a stubbed HXR.RES + editor-core, with a fake localStorage.
export function loadEditor({ seed = 1, initialLS = {} } = {}) {
  const win = {};
  const sandboxMath = Object.create(Math);
  sandboxMath.random = makeRng(seed);
  const store = new Map(Object.entries(initialLS).map(([k, v]) => [k, JSON.stringify(v)]));
  const localStorage = {
    getItem: (k) => (store.has(k) ? store.get(k) : null),
    setItem: (k, v) => { store.set(k, String(v)); },
    removeItem: (k) => { store.delete(k); },
    clear: () => store.clear(),
  };
  const sandbox = { window: win, Math: sandboxMath, console, localStorage };
  sandbox.globalThis = sandbox;
  const ctx = vm.createContext(sandbox);
  // engine + stages (pure JS)
  for (const f of ['engine.jsx', 'stages.jsx']) {
    vm.runInContext(readFileSync(join(ROOT, f), 'utf8'), ctx, { filename: f });
  }
  // resources.jsx contains JSX → stub HXR.RES with plain objects for merge tests
  win.HXR = { RES: { player: { kind: 'pixel', px: 2.4 }, drone: { kind: 'pixel', px: 2.3 } } };
  vm.runInContext(readFileSync(join(ROOT, 'editor-core.jsx'), 'utf8'), ctx, { filename: 'editor-core.jsx' });
  return { HX: win.HX, HXS: win.HXS, HXR: win.HXR, HXE: win.HXE, win, store };
}
```

- [ ] **Step 2: 임시 확인 (editor-core 생성 후 동작)** — 이 Task에서는 editor-core가 아직 없으니 다음 Task에서 함께 검증.

- [ ] **Step 3: Commit** *(git)*

```bash
git add tests/harness.mjs
git commit -m "test: add loadEditor harness (fake localStorage + RES stub)"
```

---

### Task 3: editor-core 병합/직렬화/검증 + 테스트

**Files:**
- Create: `editor-core.jsx`
- Create: `tests/overrides.test.mjs`
- Modify: `stages.jsx` (`initStageDef` 분리, `isUnlocked` 커스텀 허용)

- [ ] **Step 1: stages.jsx — `initStageDef` 분리 + `isUnlocked` 커스텀 허용**

현재 `initStage(idx)`를 def 받는 버전으로 분리:

```js
const initStageDef = (def, idx = 0) => {
  const base = {
    mode: 'stage', stage: def, stageIdx: idx,
    pl: def.start ? { ...def.start } : { r: R - 1, c: mid },
    bl: [],
    walls: (def.walls || []).map(w => ({ ...w })),
    turrets: (def.turrets || []).map(t => ({ ...t })),
    spikes: (def.spikes || []).map(sp => ({ ...sp })),
    cracks: (def.cracks || []).map(cr => ({ ...cr, broken: false })),
    pads: (def.pads || []).map(p => ({ ...p })),
    lasers: [],
    enemies: (def.enemies || []).map(e => ({ ...e })),
    goal: def.goal ? { ...def.goal } : null,
    gems: (def.gems || []).map(g => ({ ...g })),
    t: 0, sc: 0, ov: false, win: false, ln: '', its: [], fz: 0, ht: 0,
    hist: null, combo: 0, bossWaves: 0, obj: objFor(def), skillUses: 0,
    si: def.firstDelay != null ? def.firstDelay : 1, evts: [],
  };
  base.np = pickPattern(def, 0, base);
  base.np2 = pickPattern(def, 1, { ...base, bossWaves: def.type === 'boss' ? 1 : 0 });
  return base;
};
const initStage = (idx) => initStageDef(STAGES[idx], idx);
```

> 주의: `cracks`/`pads`는 확장팩 Task 11에서 이미 추가됐을 수 있다. 중복되지 않게 분리만 한다.

`isUnlocked` 교체:

```js
const isUnlocked = (idx, stars) =>
  idx === 0 || (STAGES[idx] && STAGES[idx].id >= 1000) || !!stars[STAGES[idx - 1].id];
```

export에 `initStageDef` 추가.

- [ ] **Step 2: editor-core.jsx 작성**

```js
/* editor-core.jsx — pure data layer for the editor (no JSX/React).
 * Builds window.HXB and merges localStorage overrides into STAGES / RES / HXB.
 * Loaded after stages + resources, before screens/editor/app. */

const HXE_LS = { stages: 'hex_edit_stages', balance: 'hex_edit_balance', res: 'hex_edit_res' };

const deepMerge = (base, patch) => {
  if (Array.isArray(patch)) return patch.slice();
  if (patch && typeof patch === 'object') {
    const out = (base && typeof base === 'object' && !Array.isArray(base)) ? { ...base } : {};
    for (const k of Object.keys(patch)) {
      const bv = out[k], pv = patch[k];
      out[k] = (bv && typeof bv === 'object' && !Array.isArray(bv) && pv && typeof pv === 'object' && !Array.isArray(pv))
        ? deepMerge(bv, pv) : (Array.isArray(pv) ? pv.slice() : pv);
    }
    return out;
  }
  return patch;
};

const readLS = (key, fallback) => {
  try {
    if (typeof localStorage === 'undefined') return fallback;
    const v = localStorage.getItem(key);
    return v ? JSON.parse(v) : fallback;
  } catch { return fallback; }
};
const writeLS = (key, val) => {
  try { if (typeof localStorage !== 'undefined') localStorage.setItem(key, JSON.stringify(val)); } catch {}
};

const buildBalance = (patch) => deepMerge(window.HX.DEFAULT_BAL, patch || {});

const applyStageOverrides = (baseStages, data) => {
  const overrides = (data && data.overrides) || {};
  const custom = (data && data.custom) || [];
  const merged = baseStages.map(st => (overrides[st.id] ? deepMerge(st, overrides[st.id]) : st));
  return [...merged, ...custom];
};

const applyResOverrides = (baseRes, patch) => {
  if (!patch) return { ...baseRes };
  const out = { ...baseRes };
  for (const k of Object.keys(patch)) out[k] = deepMerge(baseRes[k] || {}, patch[k]);
  return out;
};

// pristine snapshots so re-applying is idempotent
const BASE_STAGES = window.HXS.STAGES.slice();
const BASE_RES = { ...window.HXR.RES };

const applyOverrides = () => {
  const stageData = readLS(HXE_LS.stages, { overrides: {}, custom: [] });
  const balPatch = readLS(HXE_LS.balance, {});
  const resPatch = readLS(HXE_LS.res, {});
  // STAGES mutated in place (same array reference used by stages.jsx/initStage)
  const eff = applyStageOverrides(BASE_STAGES, stageData);
  window.HXS.STAGES.length = 0;
  eff.forEach(s => window.HXS.STAGES.push(s));
  // RES mutated in place
  const effRes = applyResOverrides(BASE_RES, resPatch);
  Object.keys(window.HXR.RES).forEach(k => { delete window.HXR.RES[k]; });
  Object.assign(window.HXR.RES, effRes);
  // balance
  window.HXB = buildBalance(balPatch);
};

const serializeOverrides = () => JSON.stringify({
  version: 1,
  stages: readLS(HXE_LS.stages, { overrides: {}, custom: [] }),
  balance: readLS(HXE_LS.balance, {}),
  res: readLS(HXE_LS.res, {}),
}, null, 2);

const parseOverrides = (json) => {
  const obj = JSON.parse(json); // throws on malformed JSON
  if (!obj || typeof obj !== 'object') throw new Error('payload must be an object');
  const stages = obj.stages || { overrides: {}, custom: [] };
  if (typeof stages !== 'object' || typeof (stages.overrides || {}) !== 'object' || !Array.isArray(stages.custom || []))
    throw new Error('invalid stages section');
  if (obj.res && typeof obj.res !== 'object') throw new Error('invalid res section');
  if (obj.balance && typeof obj.balance !== 'object') throw new Error('invalid balance section');
  return { stages, balance: obj.balance || {}, res: obj.res || {} };
};

const importOverrides = (json) => {
  const parsed = parseOverrides(json); // atomic: throws before any write
  writeLS(HXE_LS.stages, parsed.stages);
  writeLS(HXE_LS.balance, parsed.balance);
  writeLS(HXE_LS.res, parsed.res);
  applyOverrides();
};

// fairness/reachability heuristic via simulation
const validateStage = (def, { turns = 30 } = {}) => {
  const warnings = [];
  const HX = window.HX, HXS = window.HXS;
  let s;
  try { s = HXS.initStageDef(def, 0); }
  catch (e) { return { ok: false, warnings: ['초기화 실패: ' + e.message] }; }
  for (let i = 0; i < turns && !s.win && !s.ov; i++) {
    const opts = [{ r: s.pl.r, c: s.pl.c }, ...HX.D(s.pl.r).map(([dr, dc]) => ({ r: s.pl.r + dr, c: s.pl.c + dc }))];
    let next = null;
    for (const o of opts) {
      if (o.r < 0 || o.r >= HX.R || o.c < 0 || o.c >= HX.C) continue;
      const n = HX.tick(s, o.r, o.c);
      if (n !== s && !n.ov) { next = n; break; }
    }
    if (!next) { warnings.push(`턴 ${s.t}: 안전한 이동이 없음 — 불공정 가능`); break; }
    s = next;
  }
  return { ok: warnings.length === 0, warnings };
};

// merge any localStorage overrides on load so the game sees edited data
applyOverrides();

window.HXE = {
  LS: HXE_LS, deepMerge,
  applyOverrides, applyStageOverrides, applyResOverrides, buildBalance,
  serializeOverrides, parseOverrides, importOverrides, validateStage,
  readLS, writeLS, BASE_STAGES, BASE_RES,
};
```

- [ ] **Step 3: 데이터 레이어 테스트 작성**

`tests/overrides.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { loadEditor } from './harness.mjs';

test('buildBalance merges patch over DEFAULT_BAL', () => {
  const { HXE, HX } = loadEditor();
  const b = HXE.buildBalance({ skill: { bombCost: 99 } });
  assert.equal(b.skill.bombCost, 99);
  assert.equal(b.skill.undoCost, HX.DEFAULT_BAL.skill.undoCost); // untouched
});

test('applyStageOverrides merges by id and appends custom', () => {
  const { HXE } = loadEditor();
  const base = [{ id: 1, name: 'A', interval: 2 }, { id: 2, name: 'B', interval: 2 }];
  const out = HXE.applyStageOverrides(base, {
    overrides: { 2: { name: 'B!' } },
    custom: [{ id: 1000, name: 'Custom' }],
  });
  assert.equal(out[0].name, 'A');
  assert.equal(out[1].name, 'B!');
  assert.equal(out[2].id, 1000);
});

test('applyResOverrides patches one entry, keeps others', () => {
  const { HXE } = loadEditor();
  const base = { player: { px: 2.4 }, drone: { px: 2.3 } };
  const out = HXE.applyResOverrides(base, { player: { px: 3 } });
  assert.equal(out.player.px, 3);
  assert.equal(out.drone.px, 2.3);
});

test('serialize → parse round-trips', () => {
  const { HXE } = loadEditor({ initialLS: {
    hex_edit_balance: { skill: { freezeCost: 70 } },
    hex_edit_stages: { overrides: { 1: { name: 'X' } }, custom: [] },
  } });
  const json = HXE.serializeOverrides();
  const parsed = HXE.parseOverrides(json);
  assert.equal(parsed.balance.skill.freezeCost, 70);
  assert.equal(parsed.stages.overrides[1].name, 'X');
});

test('parseOverrides rejects malformed JSON and bad schema', () => {
  const { HXE } = loadEditor();
  assert.throws(() => HXE.parseOverrides('{not json'));
  assert.throws(() => HXE.parseOverrides(JSON.stringify({ stages: { custom: 'nope' } })));
});

test('importOverrides applies balance to window.HXB', () => {
  const { HXE, win } = loadEditor();
  HXE.importOverrides(JSON.stringify({ balance: { skill: { bombCost: 5 } } }));
  assert.equal(win.HXB.skill.bombCost, 5);
});

test('applyOverrides mutates STAGES in place (custom appended)', () => {
  const { HXE, HXS } = loadEditor({ initialLS: {
    hex_edit_stages: { overrides: {}, custom: [{ id: 1000, type: 'survive', name: 'C', interval: 2, surviveTurns: 5 }] },
  } });
  // editor-core ran applyOverrides on load
  assert.ok(HXS.STAGES.some(s => s.id === 1000));
});

test('validateStage flags a player boxed in by walls', () => {
  const { HXE } = loadEditor();
  // surround the default start (r=10,c=3) with walls on all neighbors
  const def = { id: 1000, type: 'survive', name: 'box', interval: 99, surviveTurns: 20,
    pool: [], walls: [
      { r: 10, c: 2 }, { r: 10, c: 4 }, { r: 9, c: 2 }, { r: 9, c: 3 },
    ] };
  const res = HXE.validateStage(def, { turns: 5 });
  // with most neighbors blocked and 'stay' available, it may still pass;
  // assert the function returns the documented shape regardless.
  assert.ok(typeof res.ok === 'boolean' && Array.isArray(res.warnings));
});
```

- [ ] **Step 4: 실행 → 통과 확인**

Run: `npm test`
Expected: PASS (overrides + 기존 전체).

- [ ] **Step 5: HTML에 editor-core 로드 (Hex Danmaku.html)**

`<script type="text/babel" src="sprites.jsx"></script>` 다음, `screens.jsx` 앞에:

```html
  <script type="text/babel" src="editor-core.jsx"></script>
```

> `editor-core.jsx`는 JSX가 없지만 `text/babel`로 로드해도 무방하다(다른 jsx와 로드 방식 통일).

- [ ] **Step 6: Commit** *(git)*

```bash
git add editor-core.jsx tests/overrides.test.mjs stages.jsx "Hex Danmaku.html"
git commit -m "feat(editor): pure data layer (overrides/serialize/validate) + bootstrap"
```

---

## Phase E3 — 에디터 셸 + 메뉴 진입

### Task 4: 메뉴 버튼 + 화면 라우팅 + EditorScreen 골격

**Files:**
- Modify: `screens.jsx` (`MenuScreen`)
- Modify: `app.jsx` (`editor` 화면)
- Create: `editor.jsx`
- Modify: `Hex Danmaku.html`

- [ ] **Step 1: 메뉴 버튼 (screens.jsx `MenuScreen`)**

`mode-list` 안, 엔드리스 버튼 다음에:

```js
      <button className="mode-btn editor" onClick={onEditor}>
        <span className="mb-ico">✎</span>
        <span className="mb-text">
          <span className="mb-name">에디터</span>
          <span className="mb-desc">스테이지 · 밸런스 · 리소스 편집</span>
        </span>
        <span className="mb-meta">DEV</span>
      </button>
```

`MenuScreen`의 props에 `onEditor` 추가.

- [ ] **Step 2: editor.jsx 골격 (탭 셸)**

```js
/* editor.jsx — in-game editor UI (Stage / Balance / Resource). Exports EditorScreen. */
const { useState: useStateE } = React;

const EditorScreen = ({ onExit, onTestPlay }) => {
  const [tab, setTab] = useStateE('balance'); // balance | resource | stage
  const [msg, setMsg] = useStateE('');
  const flash = (m) => { setMsg(m); setTimeout(() => setMsg(''), 1500); };

  return (
    <div className="screen editor">
      <div className="ed-bar">
        <button className="back-btn" onClick={onExit}>← 메뉴</button>
        <span className="ed-title">에디터</span>
        <div className="ed-tabs">
          {['balance', 'resource', 'stage'].map(t => (
            <button key={t} className={`ed-tab ${tab === t ? 'on' : ''}`} onClick={() => setTab(t)}>
              {t === 'balance' ? '밸런스' : t === 'resource' ? '리소스' : '스테이지'}
            </button>
          ))}
        </div>
        <EditorIO flash={flash} />
      </div>
      {msg && <div className="ed-flash">{msg}</div>}
      <div className="ed-body">
        {tab === 'balance' && <BalanceTab flash={flash} />}
        {tab === 'resource' && <ResourceTab flash={flash} />}
        {tab === 'stage' && <StageTab flash={flash} onTestPlay={onTestPlay} />}
      </div>
    </div>
  );
};

Object.assign(window, { EditorScreen });
```

> `EditorIO`, `BalanceTab`, `ResourceTab`, `StageTab`는 다음 Task들에서 정의한다. 이 Task에선 각 탭을 임시 `const BalanceTab = () => <div>밸런스(준비중)</div>;` 등 placeholder로 두고 셸이 뜨는지만 확인 후, 후속 Task에서 교체한다.

임시 placeholder (이 Task 한정):

```js
const EditorIO = () => <span className="ed-io" />;
const BalanceTab = () => <div className="ed-pane">밸런스 — 다음 단계</div>;
const ResourceTab = () => <div className="ed-pane">리소스 — 다음 단계</div>;
const StageTab = () => <div className="ed-pane">스테이지 — 다음 단계</div>;
```

- [ ] **Step 3: app.jsx — editor 화면 라우팅 + 테스트 플레이**

`App` 안:

```js
  const toEditor = useCallback(() => { setG(null); setScreen('editor'); }, []);
  const testPlay = useCallback((def) => {
    setG({ ...HXS.initStageDef(def, def.stageIdx ?? 0), _test: true });
    setScreen('play'); setRunId(n => n + 1);
  }, []);
```

`MenuScreen` 렌더에 `onEditor={toEditor}` 전달. 화면 분기에 추가:

```js
  if (screen === 'editor') {
    return <EditorScreen onExit={toMenu} onTestPlay={testPlay} />;
  }
```

`window`에서 `EditorScreen` 구조분해(app.jsx 상단):

```js
  MenuScreen, StageSelect, ClearOverlay, FailOverlay, Stars, EditorScreen,
```

- [ ] **Step 4: 테스트 플레이는 별점 미저장 (app.jsx GameView 클리어 effect)**

GameView의 스테이지 클리어 effect에서 테스트 플레이 제외:

```js
  useEffect(() => {
    if (isStage && g.win && !g._test) {
      const sNum = HXS.rateStage(g);
      setEarned(sNum);
      setStars(HXS.saveStars(g.stage.id, sNum));
    }
  }, [g.win]);
```

테스트 플레이 종료 시 목록 대신 에디터로 돌아가도록, 오버레이의 `onList`를 분기하거나 `onMenu`를 활용. 간단히: 테스트 플레이는 `onList`/`onMenu` 모두 `toEditor`로 연결되게 `GameView`에 `onExitTest` 전달은 생략하고, `_test`이면 ClearOverlay/FailOverlay의 목록 버튼이 `onMenu`(여기선 toEditor를 넘김)를 쓰도록 한다. 즉 `testPlay`로 진입한 GameView에는 `onList`/`onMenu`에 `toEditor`를 전달:

```js
  // App render of GameView: when current g._test, route exits back to editor
  onList={g && g._test ? toEditor : toSelect}
  onMenu={g && g._test ? toEditor : toMenu}
```

- [ ] **Step 5: HTML에 editor.jsx 로드 (Hex Danmaku.html, screens.jsx 다음·app.jsx 앞)**

```html
  <script type="text/babel" src="editor.jsx"></script>
```

- [ ] **Step 6: 브라우저 검증** — 메뉴 → 에디터 진입, 탭 전환, ← 메뉴 복귀.

- [ ] **Step 7: Commit** *(git)*

```bash
git add screens.jsx app.jsx editor.jsx "Hex Danmaku.html"
git commit -m "feat(editor): menu entry + screen routing + tab shell"
```

---

## Phase E4 — 밸런스 탭

### Task 5: BalanceTab

**Files:**
- Modify: `editor.jsx`

- [ ] **Step 1: BalanceTab 구현 (placeholder 교체)**

```js
// flat field list: [storageGroup, key, label, min, max, step]
const BAL_FIELDS = [
  ['skill', 'undoCost', '뒤로가기 비용', 0, 200, 5],
  ['skill', 'bombCost', '폭탄 비용', 0, 200, 5],
  ['skill', 'bombRadius', '폭탄 반경', 1, 4, 1],
  ['skill', 'freezeCost', '정지 비용', 0, 200, 5],
  ['skill', 'freezeTurns', '정지 턴', 1, 6, 1],
  ['score', 'surviveBase', '생존 점수', 0, 50, 1],
  ['score', 'comboCap', '콤보 상한', 0, 30, 1],
  ['score', 'gemBase', '별 점수', 0, 300, 5],
  ['score', 'gemCombo', '별 콤보배수', 0, 20, 1],
  ['score', 'starBase', '점수픽업 기본', 0, 200, 5],
  ['score', 'starCombo', '점수픽업 콤보배수', 0, 20, 1],
  ['item', 'spawnChance', '아이템 확률', 0, 1, 0.01],
  ['item', 'max', '아이템 최대', 0, 6, 1],
  ['item', 'pSc', '확률 점수★', 0, 1, 0.01],
  ['item', 'pBm', '확률 폭탄✸', 0, 1, 0.01],
  ['item', 'pTp', '확률 이동✦', 0, 1, 0.01],
  ['enemy', 'chaseEvery', '추적자 주기', 1, 5, 1],
  ['enemy', 'lungeWindup', '돌격수 예고', 0, 4, 1],
  ['enemy', 'lungeDash', '돌격수 돌진칸', 1, 4, 1],
  ['endless', 'diffEasy', '초급→중급 턴', 1, 60, 1],
  ['endless', 'diffNormal', '중급→고급 턴', 1, 100, 1],
  ['endless', 'diffHard', '고급→극한 턴', 1, 150, 1],
];

const BalanceTab = ({ flash }) => {
  const [bal, setBal] = useStateE(() => window.HXB || window.HX.DEFAULT_BAL);
  const set = (group, key, raw) => {
    const v = Number(raw);
    const patch = window.HXE.readLS(window.HXE.LS.balance, {});
    const next = window.HXE.deepMerge(patch, { [group]: { [key]: v } });
    window.HXE.writeLS(window.HXE.LS.balance, next);
    window.HXE.applyOverrides();
    setBal({ ...window.HXB });
  };
  const reset = () => {
    window.HXE.writeLS(window.HXE.LS.balance, {});
    window.HXE.applyOverrides();
    setBal({ ...window.HXB });
    flash('밸런스 기본값 복원');
  };
  // probability sanity
  const pSum = bal.item.pSc + bal.item.pBm + bal.item.pTp;
  return (
    <div className="ed-pane bal-pane">
      <div className="ed-row-head"><span>밸런스 파라미터</span><button className="ed-btn" onClick={reset}>기본값 복원</button></div>
      {pSum > 1 && <div className="ed-warn">아이템 확률 합({pSum.toFixed(2)}) {'>'} 1 — ht가 음수가 됩니다.</div>}
      <div className="bal-grid">
        {BAL_FIELDS.map(([group, key, label, min, max, step]) => (
          <label key={`${group}.${key}`} className="bal-field">
            <span className="bal-label">{label}</span>
            <input type="range" min={min} max={max} step={step}
              value={bal[group][key]} onChange={(e) => set(group, key, e.target.value)} />
            <input type="number" min={min} max={max} step={step} className="bal-num"
              value={bal[group][key]} onChange={(e) => set(group, key, e.target.value)} />
          </label>
        ))}
      </div>
    </div>
  );
};
```

- [ ] **Step 2: 브라우저 검증** — 슬라이더 조정 → 엔드리스/스테이지 플레이 시 즉시 반영(예: 폭탄 비용 변경 확인). 복원 동작 확인.

- [ ] **Step 3: Commit** *(git)*

```bash
git add editor.jsx
git commit -m "feat(editor): balance tab (live BAL editing)"
```

---

## Phase E5 — 리소스 탭

### Task 6: ResourceTab (픽셀 페인트 + 이미지 스왑)

**Files:**
- Modify: `editor.jsx`

- [ ] **Step 1: ResourceTab 구현**

```js
const ResourceTab = ({ flash }) => {
  const keys = Object.keys(window.HXR.RES);
  const [sel, setSel] = useStateE(keys[0]);
  const [tick, setTick] = useStateE(0); // force refresh after edits
  const entry = window.HXR.RES[sel];

  const savePatch = (patch) => {
    const cur = window.HXE.readLS(window.HXE.LS.res, {});
    const next = window.HXE.deepMerge(cur, { [sel]: patch });
    window.HXE.writeLS(window.HXE.LS.res, next);
    window.HXE.applyOverrides();
    setTick(t => t + 1);
  };
  const reset = () => {
    const cur = window.HXE.readLS(window.HXE.LS.res, {});
    delete cur[sel];
    window.HXE.writeLS(window.HXE.LS.res, cur);
    window.HXE.applyOverrides();
    setTick(t => t + 1);
    flash(`${sel} 원본 복원`);
  };

  const isPixel = entry.kind === 'pixel';
  const [paintCh, setPaintCh] = useStateE(null);

  const paintCell = (r, c) => {
    if (!isPixel || !entry.grid) return;
    const grid = entry.grid.map(row => row.split(''));
    grid[r][c] = paintCh || '.';
    savePatch({ grid: grid.map(row => row.join('')) });
  };

  return (
    <div className="ed-pane res-pane">
      <div className="res-list">
        {keys.map(k => (
          <button key={k} className={`res-item ${sel === k ? 'on' : ''}`} onClick={() => setSel(k)}>{k}</button>
        ))}
      </div>
      <div className="res-edit">
        <div className="ed-row-head"><span>{sel} · {entry.kind}</span><button className="ed-btn" onClick={reset}>복원</button></div>

        {/* kind switch */}
        <div className="res-kind">
          <button className={entry.kind === 'pixel' ? 'on' : ''} disabled={entry.kind === 'vector'}
            onClick={() => savePatch({ kind: 'pixel' })}>픽셀</button>
          <button className={entry.kind === 'image' ? 'on' : ''}
            onClick={() => savePatch({ kind: 'image', src: entry.src || 'assets/.png', w: entry.w || 34, h: entry.h || 34 })}>이미지</button>
        </div>

        {isPixel && entry.grid && (
          <>
            <div className="res-palette">
              <button className={paintCh === null ? 'on' : ''} onClick={() => setPaintCh(null)}>지움(.)</button>
              {Object.entries(entry.map || {}).map(([ch, color]) => (
                <button key={ch} className={paintCh === ch ? 'on' : ''} style={{ background: color }}
                  onClick={() => setPaintCh(ch)}>{ch}</button>
              ))}
            </div>
            <div className="res-grid">
              {entry.grid.map((row, r) => (
                <div key={r} className="res-grid-row">
                  {row.split('').map((ch, c) => (
                    <button key={c} className="res-px"
                      style={{ background: ch === '.' || ch === ' ' ? '#0e0f23' : (entry.map[ch] || '#f0f') }}
                      onClick={() => paintCell(r, c)} />
                  ))}
                </div>
              ))}
            </div>
          </>
        )}

        {entry.kind === 'image' && (
          <div className="res-image-form">
            {['src', 'w', 'h', 'dx', 'dy'].map(f => (
              <label key={f} className="res-imgf"><span>{f}</span>
                <input value={entry[f] ?? ''} onChange={(e) => savePatch({ [f]: f === 'src' ? e.target.value : Number(e.target.value) || 0 })} />
              </label>
            ))}
            <label className="res-imgf"><span>smooth</span>
              <input type="checkbox" checked={!!entry.smooth} onChange={(e) => savePatch({ smooth: e.target.checked })} /></label>
          </div>
        )}

        {entry.kind === 'vector' && <div className="ed-warn">벡터 아트는 이미지로만 스왑 가능합니다.</div>}
      </div>
    </div>
  );
};
```

- [ ] **Step 2: 브라우저 검증** — 픽셀 칠하기 → 게임에서 해당 스프라이트 변경 확인. 이미지 스왑 폼 동작. 복원 동작.

- [ ] **Step 3: Commit** *(git)*

```bash
git add editor.jsx
git commit -m "feat(editor): resource tab (pixel paint + image swap)"
```

---

## Phase E6 — 스테이지 탭

### Task 7: StageTab — 목록 + 캔버스 + 팔레트

**Files:**
- Modify: `editor.jsx`

- [ ] **Step 1: 편집 상태 모델 + 목록 + 캔버스 + 팔레트 구현**

```js
const PALETTE = [
  ['erase', '지우개'], ['wall', '벽'], ['crack', '발판'], ['pad', '컨베이어'],
  ['spike', '가시'], ['turret', '포대'], ['gem', '별'], ['goal', '게이트'],
  ['start', '시작'], ['enemy:chase', '추적자'], ['enemy:bounce', '반사체'], ['enemy:lunge', '돌격수'],
];

// produce a deep editable copy of a stage def
const cloneDef = (def) => JSON.parse(JSON.stringify(def));

const blankDef = () => ({
  id: 1000, type: 'survive', name: '커스텀', sub: '', tip: '',
  interval: 2, surviveTurns: 12, pool: [],
  walls: [], cracks: [], pads: [], spikes: [], turrets: [], enemies: [], gems: [],
  goal: null, start: null,
});

const StageTab = ({ flash, onTestPlay }) => {
  const { STAGES } = window.HXS;
  const [list, setList] = useStateE(() => STAGES.map((s, i) => ({ idx: i, id: s.id, name: s.name, custom: s.id >= 1000 })));
  const [def, setDef] = useStateE(null);     // current editable def
  const [tool, setTool] = useStateE('wall');

  const loadStage = (idx) => setDef({ ...cloneDef(STAGES[idx]), stageIdx: idx });
  const newCustom = () => {
    const maxId = Math.max(999, ...STAGES.map(s => s.id));
    setDef({ ...blankDef(), id: maxId + 1, stageIdx: STAGES.length });
  };

  const place = (r, c) => {
    if (!def) return;
    const d = cloneDef(def); d.stageIdx = def.stageIdx;
    const removeAt = (arr) => (arr || []).filter(o => !(o.r === r && o.c === c));
    // clear any existing occupant first (single-occupancy)
    d.walls = removeAt(d.walls); d.cracks = removeAt(d.cracks); d.pads = removeAt(d.pads);
    d.spikes = removeAt(d.spikes); d.turrets = removeAt(d.turrets); d.enemies = removeAt(d.enemies);
    d.gems = removeAt(d.gems);
    if (d.goal && d.goal.r === r && d.goal.c === c) d.goal = null;
    if (d.start && d.start.r === r && d.start.c === c) d.start = null;

    if (tool === 'erase') { /* already removed */ }
    else if (tool === 'wall') d.walls.push({ r, c });
    else if (tool === 'crack') d.cracks.push({ r, c });
    else if (tool === 'pad') d.pads.push({ r, c, dir: 1 });
    else if (tool === 'spike') d.spikes.push({ r, c });
    else if (tool === 'turret') d.turrets.push({ r, c, period: 3, phase: 0 });
    else if (tool === 'gem') d.gems.push({ r, c });
    else if (tool === 'goal') d.goal = { r, c };
    else if (tool === 'start') d.start = { r, c };
    else if (tool.startsWith('enemy:')) d.enemies.push({ r, c, kind: tool.split(':')[1], dir: 1 });
    setDef(d);
  };

  const save = () => {
    const cur = window.HXE.readLS(window.HXE.LS.stages, { overrides: {}, custom: [] });
    const clean = cloneDef(def); delete clean.stageIdx;
    if (def.id >= 1000) {
      const i = cur.custom.findIndex(s => s.id === def.id);
      if (i >= 0) cur.custom[i] = clean; else cur.custom.push(clean);
    } else {
      cur.overrides[def.id] = clean;
    }
    window.HXE.writeLS(window.HXE.LS.stages, cur);
    window.HXE.applyOverrides();
    setList(window.HXS.STAGES.map((s, i) => ({ idx: i, id: s.id, name: s.name, custom: s.id >= 1000 })));
    flash('스테이지 저장됨');
  };
  const validate = () => {
    const res = window.HXE.validateStage(def);
    flash(res.ok ? '검증 통과 — 안전 이동 항상 존재' : res.warnings[0]);
  };

  return (
    <div className="ed-pane stage-pane">
      <div className="stage-list">
        <button className="ed-btn" onClick={newCustom}>+ 새 커스텀</button>
        {list.map(s => (
          <button key={s.id} className={`stage-litem ${def && def.id === s.id ? 'on' : ''}`} onClick={() => loadStage(s.idx)}>
            {String(s.id).padStart(2, '0')} {s.name}{s.custom ? ' ·C' : ''}
          </button>
        ))}
      </div>

      {def && (
        <div className="stage-canvas">
          <div className="ed-palette">
            {PALETTE.map(([t, label]) => (
              <button key={t} className={`ed-tool ${tool === t ? 'on' : ''}`} onClick={() => setTool(t)}>{label}</button>
            ))}
          </div>
          <EditorBoard def={def} onCell={place} />
          <div className="stage-actions">
            <button className="ed-btn" onClick={validate}>검증</button>
            <button className="ed-btn" onClick={() => onTestPlay(def)}>테스트 ▶</button>
            <button className="ed-btn primary" onClick={save}>저장</button>
          </div>
        </div>
      )}

      {def && <StageProps def={def} setDef={setDef} />}
    </div>
  );
};
```

- [ ] **Step 2: EditorBoard (편집용 헥스 보드)**

```js
// editable board: draws the hex grid + current def occupants; click → onCell(r,c)
const EditorBoard = ({ def, onCell }) => {
  const { hc, hp, SW, SH, R, C } = window.HX;
  const keyset = (arr) => new Set((arr || []).map(o => `${o.r},${o.c}`));
  const W = keyset(def.walls), CR = keyset(def.cracks), PD = keyset(def.pads),
        SP = keyset(def.spikes), TT = keyset(def.turrets), GM = keyset(def.gems),
        EN = new Map((def.enemies || []).map(e => [`${e.r},${e.c}`, e.kind]));
  const cells = [];
  for (let r = 0; r < R; r++) for (let c = 0; c < C; c++) {
    const k = `${r},${c}`; const { x, y } = hc(r, c);
    let fill = '#1c1f3e';
    if (W.has(k)) fill = '#3a3f6e'; else if (CR.has(k)) fill = '#2a2440';
    else if (PD.has(k)) fill = '#13402c'; else if (SP.has(k)) fill = '#2e1217';
    else if (TT.has(k)) fill = '#23264a'; else if (GM.has(k)) fill = '#3a2a18';
    else if (def.goal && def.goal.r === r && def.goal.c === c) fill = '#1e1442';
    else if (def.start && def.start.r === r && def.start.c === c) fill = '#0c2942';
    else if (EN.has(k)) fill = EN.get(k) === 'bounce' ? '#0c2a3a' : EN.get(k) === 'lunge' ? '#3a1a0a' : '#3b0a1a';
    cells.push(<path key={k} d={hp(x, y)} fill={fill} stroke="#2a2e58" strokeWidth="1.2"
      style={{ cursor: 'pointer' }} onClick={() => onCell(r, c)} />);
  }
  return <svg className="ed-board" width={SW} height={SH} viewBox={`0 0 ${SW} ${SH}`}>{cells}</svg>;
};
```

- [ ] **Step 3: 브라우저 검증** — 스테이지 선택/로드, 도구로 셀 배치/지움, 저장 후 목록 반영, 새 커스텀 추가.

- [ ] **Step 4: Commit** *(git)*

```bash
git add editor.jsx
git commit -m "feat(editor): stage tab list + editable board + palette + save/validate/testplay"
```

---

### Task 8: StageProps — 속성 + 보스 페이즈 편집

**Files:**
- Modify: `editor.jsx`

- [ ] **Step 1: StageProps 구현**

```js
const STAGE_TYPES = ['normal', 'survive', 'collect', 'boss'];
const PATTERN_KEYS = Object.keys(window.HX.PAT);
const BOSS_ATKS = ['rain', 'aimed', 'pincer', 'sweep', 'sweepGap', 'full', 'converge', 'alternate', 'spread', 'laser', 'spiral', 'summon', 'mark', 'drift'];

const StageProps = ({ def, setDef }) => {
  const upd = (patch) => setDef({ ...def, ...patch });
  const phases = def.phases || [];
  const setPhase = (i, patch) => {
    const next = phases.map((p, j) => (j === i ? { ...p, ...patch } : p));
    upd({ phases: next, bossTotal: next.reduce((a, p) => a + (p.turns || 0), 0) });
  };
  const addPhase = () => { const next = [...phases, { type: 'rain', turns: 4, name: '산탄' }]; upd({ phases: next, bossTotal: next.reduce((a, p) => a + (p.turns || 0), 0) }); };
  const delPhase = (i) => { const next = phases.filter((_, j) => j !== i); upd({ phases: next, bossTotal: next.reduce((a, p) => a + (p.turns || 0), 0) }); };
  const togglePool = (k) => {
    const cur = def.pool || [];
    const has = cur.some(p => p === window.HX.PAT[k]);
    upd({ pool: has ? cur.filter(p => p !== window.HX.PAT[k]) : [...cur, window.HX.PAT[k]] });
  };

  return (
    <div className="stage-props">
      <label className="pf"><span>이름</span><input value={def.name || ''} onChange={(e) => upd({ name: e.target.value })} /></label>
      <label className="pf"><span>설명(sub)</span><input value={def.sub || ''} onChange={(e) => upd({ sub: e.target.value })} /></label>
      <label className="pf"><span>팁</span><input value={def.tip || ''} onChange={(e) => upd({ tip: e.target.value })} /></label>
      <label className="pf"><span>타입</span>
        <select value={def.type} onChange={(e) => upd({ type: e.target.value })}>
          {STAGE_TYPES.map(t => <option key={t} value={t}>{t}</option>)}
        </select></label>
      <label className="pf"><span>소환주기</span><input type="number" min="1" max="6" value={def.interval || 2} onChange={(e) => upd({ interval: Number(e.target.value) })} /></label>

      {def.type === 'survive' && (
        <label className="pf"><span>생존 턴</span><input type="number" min="1" value={def.surviveTurns || 12} onChange={(e) => upd({ surviveTurns: Number(e.target.value) })} /></label>
      )}

      {def.type !== 'boss' && (
        <div className="pf-pool">
          <span>패턴 풀</span>
          <div className="pool-keys">
            {PATTERN_KEYS.map(k => (
              <button key={k} className={(def.pool || []).some(p => p === window.HX.PAT[k]) ? 'on' : ''} onClick={() => togglePool(k)}>{window.HX.PAT[k].n}</button>
            ))}
          </div>
        </div>
      )}

      {def.type === 'boss' && (
        <div className="pf-phases">
          <div className="ed-row-head"><span>보스 페이즈 (총 {def.bossTotal || 0})</span><button className="ed-btn" onClick={addPhase}>+ 페이즈</button></div>
          {phases.map((p, i) => (
            <div key={i} className="phase-row">
              <select value={p.type} onChange={(e) => setPhase(i, { type: e.target.value })}>
                {BOSS_ATKS.map(a => <option key={a} value={a}>{a}</option>)}
              </select>
              <input className="ph-name" value={p.name || ''} onChange={(e) => setPhase(i, { name: e.target.value })} placeholder="이름" />
              <input className="ph-turns" type="number" min="1" value={p.turns || 1} onChange={(e) => setPhase(i, { turns: Number(e.target.value) })} />
              <button className="ph-del" onClick={() => delPhase(i)}>✕</button>
            </div>
          ))}
        </div>
      )}
    </div>
  );
};
```

- [ ] **Step 2: 브라우저 검증** — 속성 변경, 패턴 풀 토글, 보스 페이즈 추가/삭제·turns 변경 시 bossTotal 자동 갱신, 저장 후 테스트 플레이로 반영 확인.

- [ ] **Step 3: Commit** *(git)*

```bash
git add editor.jsx
git commit -m "feat(editor): stage props + boss phase editor"
```

---

## Phase E7 — Export / Import

### Task 9: EditorIO (export/import)

**Files:**
- Modify: `editor.jsx`

- [ ] **Step 1: EditorIO 구현 (placeholder 교체)**

```js
const EditorIO = ({ flash }) => {
  const [open, setOpen] = useStateE(false);
  const [text, setText] = useStateE('');
  const doExport = () => {
    const json = window.HXE.serializeOverrides();
    setText(json); setOpen(true);
    if (navigator.clipboard) navigator.clipboard.writeText(json).then(() => flash('클립보드 복사됨')).catch(() => {});
  };
  const doImport = () => {
    try { window.HXE.importOverrides(text); flash('가져오기 적용됨 (새로고침 권장)'); setOpen(false); }
    catch (e) { flash('가져오기 실패: ' + e.message); }
  };
  return (
    <div className="ed-io">
      <button className="ed-btn" onClick={doExport}>Export</button>
      <button className="ed-btn" onClick={() => setOpen(o => !o)}>Import</button>
      {open && (
        <div className="ed-io-panel">
          <textarea value={text} onChange={(e) => setText(e.target.value)} placeholder="여기에 JSON 붙여넣기" />
          <div className="ed-io-actions">
            <button className="ed-btn primary" onClick={doImport}>적용</button>
            <button className="ed-btn" onClick={() => setOpen(false)}>닫기</button>
          </div>
        </div>
      )}
    </div>
  );
};
```

- [ ] **Step 2: 브라우저 검증** — Export로 JSON 확인/복사, 다른 값 Import 적용 → 게임 반영, 잘못된 JSON 거부 메시지.

- [ ] **Step 3: Commit** *(git)*

```bash
git add editor.jsx
git commit -m "feat(editor): export/import JSON overrides"
```

---

## Phase E8 — 스타일 + 문서

### Task 10: 에디터 스타일

**Files:**
- Modify: `styles.css`

- [ ] **Step 1: 에디터 레이아웃 스타일 추가**

```css
.screen.editor { display: flex; flex-direction: column; height: 100%; }
.ed-bar { display: flex; align-items: center; gap: 10px; padding: 8px 12px; border-bottom: 1px solid #2a2e58; }
.ed-title { font-weight: 700; }
.ed-tabs, .ed-io { display: flex; gap: 6px; }
.ed-tab.on, .ed-tool.on, .ed-tab:hover { background: #1f3a48; }
.ed-tab, .ed-tool, .ed-btn, .res-item, .stage-litem { background: #1c1f3e; color: #cdeafe; border: 1px solid #2a2e58; border-radius: 6px; padding: 4px 8px; cursor: pointer; }
.ed-btn.primary { background: #1f3a48; border-color: #38bdf8; }
.ed-flash { position: absolute; top: 50px; left: 50%; transform: translateX(-50%); background: #0c2942; padding: 6px 12px; border-radius: 6px; z-index: 20; }
.ed-warn { color: #fb7185; font-size: 12px; margin: 4px 0; }
.ed-body { flex: 1; overflow: auto; padding: 12px; }
.bal-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px 16px; }
.bal-field { display: grid; grid-template-columns: 120px 1fr 64px; align-items: center; gap: 8px; }
.bal-num { width: 60px; }
.res-pane, .stage-pane { display: flex; gap: 12px; }
.res-list, .stage-list { display: flex; flex-direction: column; gap: 4px; min-width: 120px; max-height: 70vh; overflow: auto; }
.res-grid-row { display: flex; }
.res-px { width: 14px; height: 14px; padding: 0; border: 1px solid #0e0f23; }
.res-palette { display: flex; gap: 4px; flex-wrap: wrap; margin: 6px 0; }
.ed-palette { display: flex; gap: 4px; flex-wrap: wrap; margin-bottom: 8px; }
.stage-props { min-width: 220px; display: flex; flex-direction: column; gap: 6px; }
.pf { display: grid; grid-template-columns: 80px 1fr; gap: 6px; align-items: center; }
.phase-row { display: flex; gap: 4px; align-items: center; margin: 2px 0; }
.ph-turns { width: 48px; } .ph-name { width: 80px; }
.ed-io-panel { position: absolute; right: 12px; top: 48px; background: #14172e; border: 1px solid #2a2e58; padding: 8px; border-radius: 8px; z-index: 30; }
.ed-io-panel textarea { width: 320px; height: 180px; }
.mode-btn.editor .mb-ico { color: #a78bfa; }
```

- [ ] **Step 2: 브라우저 검증** — 세 탭 레이아웃이 깨지지 않는지.

- [ ] **Step 3: Commit** *(git)*

```bash
git add styles.css
git commit -m "style(editor): editor screen layout"
```

---

### Task 11: 문서 갱신

**Files:**
- Modify: `docs/hex-danmaku-dev.md`

- [ ] **Step 1: 에디터 섹션 추가**
- 신규 §: 에디터(메뉴 진입, 3탭), `window.HXB`(BAL) 의미, `window.HXE` 데이터 레이어, localStorage 키(`hex_edit_*`), export/import 절차, 소스 영구 반영 방법.
- §2 파일/로드 순서 표에 `editor-core.jsx`/`editor.jsx` 추가, 로드 순서 갱신.
- §14 진행도: 커스텀 스테이지(id≥1000, 항상 잠금해제), 테스트 플레이 별점 미저장 명시.

- [ ] **Step 2: Commit** *(git)*

```bash
git add docs/hex-danmaku-dev.md
git commit -m "docs: document editor, BAL, and override layer"
```

---

## Self-Review 결과 (작성자 점검)

**Spec coverage:** 스펙 §2.2(BAL)→Task1, §2.3(오버라이드/직렬화)→Task3, §2.4(화면통합)→Task4, §3(스테이지탭)→Task7-8, §4(밸런스탭)→Task5, §5(리소스탭)→Task6, §6(export/import)→Task9, §7(테스트)→Task3, §8(파일)→전 Task, §2.1(로드순서)→Task2/4. 누락 없음.

**Placeholder scan:** UI Task에 placeholder 컴포넌트는 Task4에서 임시로 두고 후속 Task에서 실제 코드로 교체함을 명시(실제 코드 모두 포함). "다음 단계" 텍스트는 Task4 한정 임시 표기로 명시.

**Type consistency:** `window.HXE`(LS/deepMerge/applyOverrides/applyStageOverrides/applyResOverrides/buildBalance/serializeOverrides/parseOverrides/importOverrides/validateStage/readLS/writeLS), `window.HXB`(BAL), `window.HX.DEFAULT_BAL`, `HXS.initStageDef(def, idx)`, localStorage 키 `hex_edit_stages|balance|res`, 스테이지 데이터 `{overrides:{[id]}, custom:[]}`, 커스텀 `id>=1000` — 전 Task 일관. `bal()` 폴백은 engine/ENEMY_KINDS 모두에서 사용.

**선행 의존 재확인:** Task6(리소스)·Task7(팔레트의 crack/pad/bounce/lunge)·Task8(보스 신패턴 드롭다운)·Task1 Step6(lunge cadence)은 **확장팩 Phase 1~6 완료**가 전제. 미완료 시 해당 항목은 비활성/오류.

**알려진 한계(문서화 대상):** validateStage는 브라우저에서 비시드 `Math.random`이라 휴리스틱(보스 랜덤 패턴은 표본). 픽셀 편집은 기존 그리드 셀만 칠하기(행/열 추가 없음).
